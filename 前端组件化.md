###   清除样式

```
用法：用于初始化样式（一般浏览器会有默认样式，会影响我们预期的效果，所以需要清除浏览器的默认样式）
注意：引入css时，这个要最先引入
```

#### CSS:

```css
/* 清除内外边距 */
body, h1, h2, h3, h4, h5, h6, hr, p,
dl, dt, dd, ul, ol, li,
pre,
fieldset, button, input, textarea,
th, td {
    margin: 0;
    padding: 0;
}
/* 设置默认字体 */
body,
button, input, select, textarea { /* for ie */
    font: 12px/1.3 "Microsoft YaHei",Tahoma, Helvetica, Arial, "\5b8b\4f53", sans-serif; /* 用 Unicode 字符表示，使得在任何编码下都无问题 */
    color: #333;
}


h1 { font-size: 18px; /* 18px / 12px = 1.5 */ }
h2 { font-size: 16px; }
h3 { font-size: 14px; }
h4, h5, h6 { font-size: 100%; }

address, cite, dfn, em, var, i{ font-style: normal; } /* 将斜体扶正 */
b, strong{ font-weight: normal; } /* 将粗体扶细 */
code, kbd, pre, samp, tt { font-family: "Courier New", Courier, monospace; } /* 统一等宽字体 */
small { font-size: 12px; } /* 小于 12px 的中文很难阅读，让 small 正常化 */

/* 重置列表元素 */
ul, ol { list-style: none; }

/* 重置文本格式元素 */
a { text-decoration: none; color: #666;}

/* 重置表单元素 */
legend { color: #000; } /* for ie6 */
fieldset, img { border: none; }
button, input, select, textarea {
    font-size: 100%; /* 使得表单元素在 ie 下能继承字体大小 */
}

/* 重置表格元素 */
table {
    border-collapse: collapse;
    border-spacing: 0;
}

/* 重置 hr */
hr {
    border: none;
    height: 1px;
}

/* 让非ie浏览器默认也显示垂直滚动条，防止因滚动条引起的闪烁 */
html { overflow-y: scroll; }

a:link:hover{
     color : lightgreen !important;
     text-decoration: underline;
 }

input{ /*去除外轮廓*/
    outline: none;
}

img{ /*去除行内元素换行符问题*/
    display: block;
}
```



### 头部导航栏（样式)

效果：

![image-20221107212657137.png](https://s2.loli.net/2022/11/10/cyqBIvT918sb2tH.png)

#### HTML：

```html
<div class="headTop">
    <div class="headTopMain">
      <!--左边-->
      <div class="left">
        <h5>欢迎来到尚优选！ 请</h5>
        <p>
          <a href="javascript:;">登录</a>
          <a href="javascript:;">注册</a>
        </p>
      </div>
      <!--右边-->
      <div class="right">
        <nav>
          <a href="##">我的订单</a>
          <a href="##">我的购物车</a>
          <a href="##">我的尚优选</a>
          <a href="##">尚优选会员</a>
          <a href="##">企业采购</a>
          <a href="##">关注尚优选</a>
          <a href="##">合作招商</a>
          <a href="##">商家后台</a>
        </nav>
      </div>
    </div>
  </div>
```

#### CSS:

```css
.headTop {
  background: #eaeaea;
  height: 30px;
}
.headTop .headTopMain {
  width: 1200px;
  height: 30px;
  line-height: 30px;
  margin: 0 auto;
}
.headTop .headTopMain .left {
  float: left;
}
.headTop .headTopMain .left h5 {
  float: left;
}
.headTop .headTopMain .left p {
  float: left;
}
.headTop .headTopMain .left p a:first-child {
  margin-left: 5px;
  border-right: 1px solid #b3aeae;
  padding-right: 5px;
}
.headTop .headTopMain .right {
  float: right;
}
.headTop .headTopMain .right nav {
  font-size: 0px;
}
.headTop .headTopMain .right nav a {
  font-size: 12px;
  margin-right: 10px;
  padding-right: 10px;
  border-right: 1px solid #b3aeae;
}
.headTop .headTopMain .right nav a:last-child {
  margin-right: 0px;
  padding-right: 0px;
  border-right: none;
}
```



### 搜索框以及logo区域

```
用法：
1.修改图片：找到“logo图片”，修改<img src="" alt="">中src的值
2.
```

效果：

![image-20221107213403922.png](https://s2.loli.net/2022/11/10/ewN7TtlUPMRqS5h.png)

#### HTML:

```html
<div class="headBottom">
    <div class="headBottomMain">
      <!--logo-->
      <h1 class="logo">
      <!-- 点击跳转  -->
        <a href="javascript:;">
        <!-- logo图片 -->
          <img src="" alt="">
        </a>
      </h1>
      <!--搜索框-->
      <div class="search">
        <!-- input 中的提示文本信息-->
        <input type="text" placeholder="请输入搜索内容">
        <input type="button" value="搜索">
      </div>
    </div>
  </div>
```

#### CSS:

```css
.headBottom .headBottomMain {
  width: 1200px;
  margin: 0 auto;
}

.headBottom .headBottomMain::after {
  clear: both;
  display: block;
  content: '';
}

.headBottom .headBottomMain .logo {
  float: left;
}

.headBottom .headBottomMain .logo img {
  width: 175px;
  height: 56px;
  margin: 25px 45px;
}

.headBottom .headBottomMain .search {
  float: right;
  margin-top: 35px;
}

.headBottom .headBottomMain .search input {
  float: left;
  box-sizing: border-box;
  width: 490px;
  height: 32px;
  padding: 0px 4px;
  border: 3px solid #ea4a36;
  color: blue;
}

.headBottom .headBottomMain .search input:last-child {
  width: 68px;
  height: 32px;
  background: #ea4a36;
  color: #fff;
  border: none;
}

.headBottom .headBottomMain .search input::-webkit-input-placeholder {
  color: yellowgreen;
}
```



### 父类导航条（样式）

```
注意：
这里要修改标题的话要自己手动在源码上改
```

#### 效果：

![image-20221107214044427.png](https://s2.loli.net/2022/11/10/azIGnM3b7vEjNFP.png)

#### HTML:

```html
<div id="navWrap">
    <!--版心元素-->
    <div class="navWrapMain">
      <a href="javascript:;">全部商品分类</a>
      <a href="javascript:;">服装城</a>
      <a href="javascript:;">美妆馆</a>
      <a href="javascript:;">尚优选超市</a>
      <a href="javascript:;">全球购</a>
      <a href="javascript:;">闪购</a>
      <a href="javascript:;">团购</a>
      <a href="javascript:;">有趣</a>
      <a href="javascript:;">秒杀</a>
    </div>
  </div>
```

#### CSS:

```css
#navWrap {
  border-bottom: 2px solid #e1251b;
}
#navWrap .navWrapMain {
  width: 1200px;
  margin: 0 auto;
}
#navWrap .navWrapMain::after {
  clear: both;
  display: block;
  content: '';
}
#navWrap .navWrapMain a {
  margin: 0 22px;
  font-size: 16px;
  color: #333;
  float: left;
  line-height: 50px;
}
#navWrap .navWrapMain a:first-child {
  width: 210px;
  height: 50px;
  background: #e1251b;
  color: #fff;
  font-size: 14px;
  font-weight: bold;
  float: left;
  text-align: center;
  line-height: 50px;
}
```



### 导航条数据（动态）

```
用法：
1.在path数组里，每一个对象代表一组数据，title为显示的值，url为跳转的地址

注意：
1.前几项都是可跳转的具有url的数据，最后一项是固定值且没有跳转功能（如上图的“iphone 6S系” 没有跳转功能）
```

#### 效果1：

![image-20221108112926441.png](https://s2.loli.net/2022/11/10/JSeL8cNfmi4dEAh.png)

#### 效果2：

![image-20221108112837602.png](https://s2.loli.net/2022/11/10/KZFt5D8kGEahcdC.png)

#### HTML:

```html
<div id="navPath"></div>
```

#### CSS:

```css
#navPath {
  padding: 9px 15px 9px 0;
  font-size: 0px;
}

#navPath a {
  font-size: 12px;
}

#navPath i {
  padding: 0 5px;
  font-size: 12px;
}
```

#### JavaScript：

```js
//数据
var goodData = {
  path: [
    {
      title: "手机、数码、通讯",
      url: "###"
    },
    {
      title: "手机",
      url: "###"
    }, {
      title: "Apple苹果",
      url: "###"
    }, {
      title: "iphone 6S系",
    }
  ]
  }
  
	//逻辑
/**
     * 思路：
     * 1、先获取路径导航的页面元素（navPath）
     * 2、再来获取所需要的数据（data.js->goodData.path）
     * 3、由于数据是需要动态产生的，那么相应的DOM元素也应该是动态产生的，含义需要根据数据的数量来进行创建DOM元素
     * 4、在遍历数据创建DOM元素的最后一条，只创建a标签，而不创建i标签
*/
   window.onload = function () {
    var navPath = document.querySelector('#navPath')
    console.log(navPath)

    var path = goodData.path
    console.log(path)

    for (var i = 0; i < path.length; i++) {
      // 最后一个元素，不需要添加url，所以下面有个if判断
      if (i == path.length - 1) {
        //创建没有href属性的a元素
        var aNode = document.createElement("a");
        aNode.innerText = path[i].title;
        navPath.appendChild(aNode);
      }
      else {
        //4.创建a元素，有href属性
        var aNode = document.createElement("a");
        aNode.href = path[i].url;
        aNode.innerText = path[i].title;

        //5.创建i元素（因为i为行内元素，并且这里只需要添加 “/” ）
        var iNode = document.createElement('i');
        iNode.innerText = '/';

        //6.给navPath盒子添加子元素
        navPath.appendChild(aNode);
        navPath.appendChild(iNode);
      }
    }
  }
```



### 放大镜功能（仅仅为模拟）

#### 效果：

![image-20221108173440505.png](https://s2.loli.net/2022/11/10/ZNUGXaVz3AeyDmW.png)

```
作用：
模拟放大镜。

注意：
1.要自己手动引入css和JavaScript！！！
2.小图片和大图片的素材要自己找，切记要找到一模一样的图片，要不然这看起来根本就不是放大镜！
3.建议图片选择：小图片400x400	大图片800x800	总之最好就是倍数
```

#### HTML:

```html
  <div id="left">
    <!--上边-->
    <div id="leftTop">
      <!--小图框-->
      <div id="smallPic">
        <!--小图片-->
        <img src="images/s1.png" alt="">
        <!--蒙版元素-->
      </div>
      <!--大图框-->
    </div>
    <!--下边-->
  </div>
```

#### CSS:

```css
#left {
  width: 400px;
  float: left;
}
#left #leftTop {
  width: 400px;
  position: relative;
}
#left #leftTop #smallPic {
  width: 400px;
  height: 400px;
  border: 1px solid #dfdfdf;
  position: relative;
}
#left #leftTop #smallPic .mask {
  width: 200px;
  height: 200px;
  background: rgba(255, 255, 255, 0.5);
  border: 1px solid #ddd;
  position: absolute;
  left: 0px;
  top: 0px;
}
#left #leftTop #bigPic {
  width: 400px;
  height: 400px;
  border: 1px solid #ddd;
  left: 420px;
  top: 0px;
  position: absolute;
  overflow: hidden;
}
#left #leftTop #bigPic img {
  width: 800px;
  height: 800px;
  position: absolute;
  left: 0px;
  top: 0px;
}

```

#### JavaScript：

```js
/**
         * 思路：
         * 1、获取小图框元素对象，并且设置移入事件(onmouseenter)
         * 2、动态的创建蒙版元素以及大图框和大图片元素
         * 3、移出时(onmouseleave)需要移除蒙版元素和大图框
*/
//1.获取小图框元素
var smallPic = document.querySelector('#left #leftTop #smallPic');
//获取leftTop元素
var leftTop = document.querySelector('#left #leftTop');
//2.设置移入事件
smallPic.onmouseenter = function () {

  //3. 创建蒙版元素
  var maskDiv = document.createElement('div');
  maskDiv.className = "mask";

  //4.创建大图框元素
  var BigPic = document.createElement('div');
  BigPic.id = "bigPic";

  //5.创建大图片元素
  var BigImg = document.createElement('img');
  BigImg.src = "images/b1.png";

  //6.大图框来追加大图片
  BigPic.appendChild(BigImg);

  //7.让小图框来追加蒙版元素
  smallPic.appendChild(maskDiv);

  //8.让leftTop元素追加大图框
  leftTop.appendChild(BigPic);


  //设置移动事件
  smallPic.onmousemove = function (event) {
    //event.clientX: 鼠标点距离浏览器左侧X轴的值
    //getBoundingClientRect().left:小图框元素距离浏览器左侧可视left值
    //offsetWidth:为元素的占位宽度
    var left = event.clientX - smallPic.getBoundingClientRect().left - maskDiv.offsetWidth / 2;
    var top = event.clientY - smallPic.getBoundingClientRect().top - maskDiv.offsetHeight / 2;

    //判断
    if (left < 0) {
      left = 0;
    } else if (left > smallPic.clientWidth - maskDiv.offsetWidth) {
      left = smallPic.clientWidth - maskDiv.offsetWidth;
    }

    if (top < 0) {
      top = 0;
    } else if (top > smallPic.clientHeight - maskDiv.offsetHeight) {
      top = smallPic.clientHeight - maskDiv.offsetHeight;
    }

    //设置left和top属性
    maskDiv.style.left = left + "px";
    maskDiv.style.top = top + "px";

    //移动的比例关系 = 蒙版元素移动的距离  /  大图片元素移动的距离
    //蒙版元素移动的距离 = 小图框宽度 – 蒙版元素的宽度
    //大图片元素移动的距离 = 大图片宽度 – 大图框元素的宽度

    var scale = (smallPic.clientWidth - maskDiv.offsetWidth) / (BigImg.offsetWidth - BigPic.clientWidth);

    console.log(scale);  //0.495

    BigImg.style.left = -left / scale + "px";
    BigImg.style.top = -top / scale + "px";
  }


  //设置移出事件
  smallPic.onmouseleave = function () {

    //让小图框移除蒙版元素
    smallPic.removeChild(maskDiv);

    //让leftTop元素移除大图框
    leftTop.removeChild(BigPic);
  }
}
```



### 点击缩略图切换图片（动态）

#### 效果：

![image-20221108173652431](C:\Users\28154\AppData\Roaming\Typora\typora-user-images\image-20221108173652431.png) 

![image-20221108173734122.png](https://s2.loli.net/2022/11/10/X6qexUE8H52a7OJ.png) 

```
用法：
1.想要多少张缩略图就去goodData的imagessrc里按照格式添加
2.功能为点击下方缩略图就可以切换上方的图片
```

#### HTML:

```HTML
  <div id="left">
    <!--上边-->
    <div id="leftTop">
      <!--小图框-->
      <div id="smallPic">
        <!--小图片-->
        <img src="images/s1.png" alt="">
        <!--蒙版元素-->
      </div>
      <!--大图框-->
    </div>
    <!--下边-->
    <div id="leftBottom">
      <a href="javascript:;">
      </a>
      <div id="piclist">
        <ul></ul>
      </div>
      <a href="javascript:;"> > </a>
    </div>
```

#### CSS:

```CSS
#left {
  width: 400px;
  float: left;
}
#left #leftTop {
  width: 400px;
  position: relative;
}
#left #leftTop #smallPic {
  width: 400px;
  height: 400px;
  border: 1px solid #dfdfdf;
  position: relative;
}
#left #leftTop #smallPic .mask {
  width: 200px;
  height: 200px;
  background: rgba(255, 255, 255, 0.5);
  border: 1px solid #ddd;
  position: absolute;
  left: 0px;
  top: 0px;
}
#left #leftTop #bigPic {
  width: 400px;
  height: 400px;
  border: 1px solid #ddd;
  left: 420px;
  top: 0px;
  position: absolute;
  overflow: hidden;
}
#left #leftTop #bigPic img {
  width: 800px;
  height: 800px;
  position: absolute;
  left: 0px;
  top: 0px;
}
#left #leftBottom {
  width: 400px;
  margin-top: 5px;
}
#left #leftBottom a {
  width: 10px;
  height: 54px;
  border: 1px solid #ccc;
  background: #ebebeb;
  text-align: center;
  line-height: 54px;
  float: left;
}
#left #leftBottom a:first-child {
  margin-right: 4px;
}
#left #leftBottom #piclist {
  width: 372px;
  height: 56px;
  float: left;
  overflow: hidden;
}
#left #leftBottom #piclist ul {
  white-space: nowrap;
  font-size: 0px;
}
#left #leftBottom #piclist ul li {
  width: 50px;
  height: 50px;
  border: 1px solid #ccc;
  padding: 2px;
  margin-right: 20px;
  display: inline-block;
}
#left #leftBottom #piclist ul li img {
  width: 50px;
  height: 50px;
}

```

#### JavaScript：

```js
//数据
var goodData = {
  imagessrc: [
    { b: "./images/b1.png", s: "./images/s1.png" },
    { b: "./images/b2.png", s: "./images/s2.png" },
    { b: "./images/b3.png", s: "./images/s3.png" },
    { b: "./images/b1.png", s: "./images/s1.png" }
  ]
}

//逻辑 
//声明一个记录点击的缩略图下标
  var bigimgIndex = 0;


  //动态渲染放大镜缩略图的数据
  thumbnailData();
  function thumbnailData() {
    /**
     * 思路：
     * 1、先获取piclist元素下的ul
     * 2、在获取data.js文件下的goodData->imagessrc
     * 3、遍历数组，根据数组的长度来创建li元素
     * 4、让ul遍历追加li元素
     */

    //1.获取piclist下的ul
    var ul = document.querySelector('#left #leftBottom #piclist ul');

    //2.获取imagessrc数据
    var imagessrc = goodData.imagessrc;

    //3.遍历数组
    for (var i = 0; i < imagessrc.length; i++) {
      //4.创建li元素
      var newLi = document.createElement('li');

      //5.创建img元素
      var newImg = document.createElement('img');
      newImg.src = imagessrc[i].s;

      //6.让li追加img元素
      newLi.appendChild(newImg);

      //7.让ul追加li元素
      ul.appendChild(newLi);
    }
  }

  //点击缩略图的效果
  thumbnailClick();
  function thumbnailClick() {
    /**
     * 思路：
     * 1、获取所有的li元素，并且循环发生点击事件
     * 2、点击缩略图需要确定其下标位置来找到对应小图路径和大图路径替换现有src的值
     */

    //1.获取所有的li元素
    var liNodes = document.querySelectorAll('#left #leftBottom #piclist ul li');

    var smallPic_img = document.querySelector('#left #leftTop #smallPic img');

    var imagessrc = goodData.imagessrc;

    //小图路径需要默认和imagessrc的第一个元素小图的路径是一致的
    smallPic_img.src = imagessrc[0].s;

    //2.循环点击这些li元素
    for (var i = 0; i < liNodes.length; i++) {
      //在点击事件之前，给每一个元素都添加上自定义的下标
      liNodes[i].index = i; /** 还可以通过setAttribute('index',i) */
      liNodes[i].onclick = function () {
        var idx = this.index; /** 事件函数中的this永远指向的是实际发生事件的目标源对象 */
        bigimgIndex = idx;

        //变换小图路径
        smallPic_img.src = imagessrc[idx].s;
      }
    }
  }
```



### 点击箭头实现图标滑动（动态）

效果：

![image-20221108195852022](C:\Users\28154\AppData\Roaming\Typora\typora-user-images\image-20221108195852022.png) 

![image-20221108195907985.png](https://s2.loli.net/2022/11/10/ijyH78KEPp51UnY.png)

```
用法：
1.想要多少项就去goodData的imagessrc里按照格式添加
2.点击左右图标可实现左右滑动
```

#### HTML:

```HTML
 <div id="left">
    <!--下边-->
    <div id="leftBottom">
      <a href="javascript:;" class="prev">
        < </a>
          <div id="piclist">
            <ul></ul>
          </div>
          <a href="javascript:;" class="next"> > </a>
    </div>
  </div>
```

#### CSS:

```CSS
#left {
    width: 400px;
    float: left;
  }

  #left #leftBottom {
    width: 400px;
    margin-top: 5px;
  }

  #left #leftBottom a {
    width: 10px;
    height: 54px;
    border: 1px solid #ccc;
    background: #ebebeb;
    text-align: center;
    line-height: 54px;
    float: left;
  }

  #left #leftBottom a:first-child {
    margin-right: 4px;
  }

  #left #leftBottom #piclist {
    width: 372px;
    height: 56px;
    float: left;
    overflow: hidden;
    position: relative;
  }

  #left #leftBottom #piclist ul {
    white-space: nowrap;
    position: absolute;
    left: 0px;
    transition: 0.5s;
  }

  #left #leftBottom #piclist ul li {
    width: 50px;
    height: 50px;
    border: 1px solid #ccc;
    padding: 2px;
    margin-right: 20px;
    display: inline-block;
  }

  #left #leftBottom #piclist ul li img {
    width: 50px;
    height: 50px;
  }
```

#### JavaScript：

```js
//数据
    var goodData = {
      imagessrc: [
        { b: "./images/b1.png", s: "./images/s1.png" },
        { b: "./images/b2.png", s: "./images/s2.png" },
        { b: "./images/b3.png", s: "./images/s3.png" },
        { b: "./images/b1.png", s: "./images/s1.png" },
        { b: "./images/b2.png", s: "./images/s2.png" },
        { b: "./images/b3.png", s: "./images/s3.png" },
        { b: "./images/b1.png", s: "./images/s1.png" },
        { b: "./images/b2.png", s: "./images/s2.png" },
        { b: "./images/b3.png", s: "./images/s3.png" },
        { b: "./images/b1.png", s: "./images/s1.png" },
        { b: "./images/b2.png", s: "./images/s2.png" },
        { b: "./images/b3.png", s: "./images/s3.png" },
        { b: "./images/b1.png", s: "./images/s1.png" },
        { b: "./images/b2.png", s: "./images/s2.png" }
      ]
    }

    //逻辑 
    //声明一个记录点击的缩略图下标
    var bigimgIndex = 0;

    //动态渲染放大镜缩略图的数据
    thumbnailData();
    function thumbnailData() {
      /**
       * 思路：
       * 1、先获取piclist元素下的ul
       * 2、在获取data.js文件下的goodData->imagessrc
       * 3、遍历数组，根据数组的长度来创建li元素
       * 4、让ul遍历追加li元素
       */

      //1.获取piclist下的ul
      var ul = document.querySelector('#left #leftBottom #piclist ul');

      //2.获取imagessrc数据
      var imagessrc = goodData.imagessrc;

      //3.遍历数组
      for (var i = 0; i < imagessrc.length; i++) {
        //4.创建li元素
        var newLi = document.createElement('li');

        //5.创建img元素
        var newImg = document.createElement('img');
        newImg.src = imagessrc[i].s;

        //6.让li追加img元素
        newLi.appendChild(newImg);

        //7.让ul追加li元素
        ul.appendChild(newLi);
      }
    }

    //点击缩略图左右箭头的效果
    thumbnailLeftRightClick();
    function thumbnailLeftRightClick() {
      /**
       * 思路：
       * 1、先获取左右两端的箭头按钮
       * 2、在获取可视的div以及ul元素和所有的li元素
       * 3、计算（发生起点、步长、总体运动的距离值）
       * 4、然后再发生点击事件
       */

      //1、获取箭头元素
      var prev = document.querySelector('#left #leftBottom a.prev');
      var next = document.querySelector('#left #leftBottom a.next');

      //2、获取可视的div以及ul元素和所有的li元素
      var ul = document.querySelector('#left #leftBottom #piclist ul');

      var liNodes = document.querySelectorAll('#left #leftBottom #piclist ul li');

      //3、计算

      //发生起点
      var start = 0;

      //步长
      var step = (liNodes[0].offsetWidth + 20) * 2;

      //总体运动的距离值 = ul的宽度 - div框的宽度 = (图片的总数 - div中显示的数量) * （li的宽度 + 20）
      var endPostion = (liNodes.length - 5) * (liNodes[0].offsetWidth + 20);

      //4、发生事件
      prev.onclick = function () {
        start -= step;
        if (start < 0) {
          start = 0;
        }
        ul.style.left = -start + "px";
      }

      next.onclick = function () {
        start += step;
        if (start > endPostion) {
          start = endPostion;
        }
        ul.style.left = -start + "px";
      }
    }
```



### 商品介绍（样式）

效果：

![image-20221108200839121.png](https://s2.loli.net/2022/11/10/1B7KWzqiy8mbanP.png)



#### HTML:

```HTML
  <!--右侧商品详情区域开始-->
  <div class="right">
    <!--商品详情数据结构开始-->
    <div class="rightTop">
      <h3>Apple iPhone 6s（A1700）64G玫瑰金色 移动通信电信4G手机bbb123</h3>
      <p>推荐选择下方[移动优惠购],手机套餐齐搞定,不用换号,每月还有花费返</p>
      <div class="priceWrap">
        <div class="priceTop">
          <span>价&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;格</span>
          <div class="price">
            <span>￥</span>
            <p>5299</p>
            <i>降价通知</i>
          </div>
          <p>
            <span>累计评价</span>
            <span>670000</span>
          </p>
        </div>
        <div class="priceBottom">
          <span>促&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;销</span>
          <p>
            <span>加价购</span>
            <span>满999.00另加20.00元，或满1999.00另加30.00元，或满2999.00另加40.00元，即可在购物车换购热销商品</span>
          </p>
        </div>
      </div>
      <div class="support">
        <span>支&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;持</span>
        <p>以旧换新，闲置手机回收 4G套餐超值抢 礼品购</p>
      </div>
      <div class="address">
        <span>配&nbsp;送&nbsp;至</span>
        <p>广东省 深圳市 宝安区</p>
      </div>
    </div>
    <!--商品详情数据结构结束-->
  </div>
```

#### CSS:

```CSS
.right {
  width: 700px;
  float: right;
}
.right .rightTop h3 {
  font-size: 14px;
  line-height: 21px;
  margin-top: 15px;
}
.right .rightTop > p {
  color: #e12228;
  margin-top: 15px;
}
.right .rightTop .priceWrap {
  margin-top: 10px;
  line-height: 28px;
  background: #fee9ec;
  padding: 7px;
}
.right .rightTop .priceWrap .priceTop::after {
  clear: both;
  display: block;
  content: '';
}
.right .rightTop .priceWrap .priceTop > span {
  margin-right: 15px;
  float: left;
}
.right .rightTop .priceWrap .priceTop .price {
  float: left;
  color: #cc1122;
}
.right .rightTop .priceWrap .priceTop .price > span {
  font-size: 16px;
  float: left;
}
.right .rightTop .priceWrap .priceTop .price p {
  font-size: 24px;
  float: left;
  font-weight: bold;
}
.right .rightTop .priceWrap .priceTop .price i {
  font-size: 12px;
  color: #cc1122;
  float: left;
}
.right .rightTop .priceWrap .priceTop > p {
  float: right;
}
.right .rightTop .priceWrap .priceBottom::after {
  clear: both;
  display: block;
  content: '';
}
.right .rightTop .priceWrap .priceBottom > span {
  float: left;
  margin-right: 15px;
}
.right .rightTop .priceWrap .priceBottom p {
  width: 520px;
  line-height: 28px;
  float: left;
}
.right .rightTop .priceWrap .priceBottom p > span {
  color: #999;
}
.right .rightTop .priceWrap .priceBottom p > span:first-child {
  background: #c81623;
  padding: 3px;
  color: #fff;
}
.right .rightTop .support {
  margin-top: 10px;
  line-height: 28px;
}
.right .rightTop .support::after {
  clear: both;
  display: block;
  content: '';
}
.right .rightTop .support > span {
  margin-right: 15px;
  float: left;
}
.right .rightTop .support > p {
  float: left;
  color: #999;
}
.right .rightTop .address {
  border-bottom: 1px solid #ededed;
  padding-bottom: 5px;
  line-height: 28px;
  margin-top: 10px;
}
.right .rightTop .address::after {
  clear: both;
  display: block;
  content: '';
}
.right .rightTop .address > span {
  margin-right: 15px;
  float: left;
}
.right .rightTop .address > p {
  float: left;
  color: #999;
}
```



### 商品参数（动态）

效果：

![image-20221108203409377.png](https://s2.loli.net/2022/11/10/V6D2oRSdjApCUnl.png) 

![image-20221108210004922.png](https://s2.loli.net/2022/11/10/VqTiLeZnbG5Ev38.png) 

```
用法：
1.可以在crumbData里修改数据
2.效果为可以选择参数
```

#### HTML:

```html
  <div id="right">
    <!--商品参数的结构布局开始-->
    <div class="rightBottom">
      <!--选择之后结果的区域开始-->
      <!--选择之后结果的区域结束-->

      <!--商品参数的选择区域开始-->
      <div class="chooseWrap"></div>
      <!--商品参数的选择区域结束-->

    </div>
    <!--商品参数的结构布局结束-->
  </div>
```

#### CSS:

```css
#right {
  float: right;
  width: 700px;
}
#right .rightBottom {
  margin-top: 10px;
  line-height: 28px;
}
#right .rightBottom .chooseWrap dl {
  margin-top: 13px;
  margin-bottom: 13px;
}
#right .rightBottom .chooseWrap dl::after {
  clear: both;
  display: block;
  content: '';
}
#right .rightBottom .chooseWrap dl dt {
  margin-right: 15px;
  float: left;
}
#right .rightBottom .chooseWrap dl dd {
  float: left;
  margin-right: 5px;
  color: #666;
  line-height: 24px;
  padding: 2px 14px;
  border-top: 1px solid #eee;
  border-left: 1px solid #eee;
  border-right: 1px solid #bbb;
  border-bottom: 1px solid #bbb;
}
#right .rightBottom .chooseWrap dl dd:first-of-type {
  color: red;
}

```

#### JavaScript：

```js
//数据 
var goodData = {
    goodsDetail: {
      crumbData:
        [
          {
            "title": "选择颜色",
            "data": [
              {
                type: "金色",
                changePrice: 0
              },
              {
                type: "银色",
                changePrice: 40
              },
              {
                type: "黑色",
                changePrice: 90
              },
            ]
          },
          {
            "title": "内存容量",
            "data": [
              {
                type: "16G",
                changePrice: 0
              },
              {
                type: "64G",
                changePrice: 300
              },
              {
                type: "128G",
                changePrice: 900
              },
              {
                type: "256G",
                changePrice: 1300
              },
            ]
          },
          {
            "title": "选择版本",
            "data": [
              {
                type: "公开版",
                changePrice: 0
              },
              {
                type: "移动版",
                changePrice: -1000
              }
            ]
          },
          {
            "title": "购买方式",
            "data": [
              {
                type: "官方标配",
                changePrice: 0
              },
              {
                type: "优惠移动版",
                changePrice: -240
              },
              {
                type: "电信优惠版",
                changePrice: -390
              },
            ]
          }
        ]
    }
  }

  //逻辑
  //商品参数数据的动态渲染
  rightBottomData();
  function rightBottomData() {
    /**
     * 思路：
     * 1、找rightBottom的元素对象
     * 2、查找data.js->goodData.goodsDetail.crumbData数据
     * 3、由于数据是一个数组，需要遍历，有一个元素则需要有一个动态的dl元素对象(dt、dd)
     */

    //1、查找元素对象
    var chooseWrap = document.querySelector('#right .rightBottom .chooseWrap');

    //2、查找数据
    var crumbData = goodData.goodsDetail.crumbData;

    //3、循环数据
    for (var i = 0; i < crumbData.length; i++) {

      //4、创建dl元素对象
      var dlNode = document.createElement('dl');

      //5、创建dt元素对象
      var dtNode = document.createElement('dt');
      dtNode.innerText = crumbData[i].title;

      //6、dl追加dt
      dlNode.appendChild(dtNode);

      //7、遍历crumbData->data元素
      for (var j = 0; j < crumbData[i].data.length; j++) {

        //创建dd元素
        var ddNode = document.createElement('dd');
        ddNode.innerText = crumbData[i].data[j].type;

        //让dl来追加dd
        dlNode.appendChild(ddNode);
      }

      //8、让chooseWrap追加dl
      chooseWrap.appendChild(dlNode);
    }
  }

  //点击商品参数之后的颜色排他效果
  clickddBind();
  function clickddBind() {
    /**
     * 思路：
     * 1、获取所有的dl元素，取其中第一个dl元素下的所有dd先做测试，
     *   测试完毕之后在对应dl第一行下标的前面在嵌套一个for循环，目的在变换下标
     * 2、循环所有的dd元素并且添加点击事件
     * 3、确定实际发生事件的目标源对象设置其文字颜色为红色，然后给其他所有的元素颜色都重置为基础颜色(#666)
     */

    //1、找第一个dl下的所有的dd元素
    var dlNodes = document.querySelectorAll('#right .rightBottom .chooseWrap dl');

    for (var i = 0; i < dlNodes.length; i++) {

      (function (i) {

        var ddNodes = dlNodes[i].querySelectorAll('dd');

        //2、遍历当前所有的dd元素
        for (var j = 0; j < ddNodes.length; j++) {

          ddNodes[j].onclick = function () {
            //  console.log(ddNodes[i]); // undefined
            //this：表示哪一个元素真实的发生了事件
            // console.log(this);

            for (var k = 0; k < ddNodes.length; k++) {
              ddNodes[k].style.color = "#666";
            }

            //假设点击的是第二个元素，下标为1'
            //ddNodes[1].style.color = "red";
            //相同下标的dd元素的字体颜色在进行覆盖操作,而其他未点击的元素都是在进行重新设置颜色
            this.style.color = "red";
          }
        }
        console.log("============")
      })(i)
    }
  }

```



### 点击参数动态形成结果

效果：

![image-20221108212318979.png](https://s2.loli.net/2022/11/10/MyOmeKT7uz3t4WP.png) 

![image-20221108212333702](C:\Users\28154\AppData\Roaming\Typora\typora-user-images\image-20221108212333702.png) 

点×号删除

![image-20221108212400312.png](https://s2.loli.net/2022/11/10/ztfRjNwJqairhDk.png) 

```
用法：
1.改功能拥有《商品参数（动态）》的所有效果
2.在其基础上添加了结果合成功能，以及结果参数的删除功能
```

#### HTML:

```HTML
<!--右侧商品详情区域开始-->
          <div id="right">
            <!--商品参数的结构布局开始-->
            <div class="rightBottom">
              <!--选择之后结果的区域开始-->
              <div class="choose"></div>
              <!--选择之后结果的区域结束-->

              <!--商品参数的选择区域开始-->
              <div class="chooseWrap"></div>
              <!--商品参数的选择区域结束-->

              <!--加入购物车以及数量区域开始-->
              <!--加入购物车以及数量区域结束-->
            </div>
            <!--商品参数的结构布局结束-->
          </div>
```

#### CSS:

```CSS
.rightBottom {
  margin-top: 10px;
  line-height: 28px;
}
.rightBottom .choose::after {
  clear: both;
  display: block;
  content: '';
}
.rightBottom .choose .mark {
  height: 30px;
  background: snow;
  border: 1px solid #ddd;
  padding: 0 20px;
  margin-right: 20px;
  text-align: center;
  line-height: 30px;
  float: left;
}
.rightBottom .choose .mark a {
  color: red;
  margin-left: 20px;
}
.rightBottom .chooseWrap dl {
  margin-top: 13px;
  margin-bottom: 13px;
}
.rightBottom .chooseWrap dl::after {
  clear: both;
  display: block;
  content: '';
}
.rightBottom .chooseWrap dl dt {
  margin-right: 15px;
  float: left;
}
.rightBottom .chooseWrap dl dd {
  float: left;
  margin-right: 5px;
  color: #666;
  line-height: 24px;
  padding: 2px 14px;
  border-top: 1px solid #eee;
  border-left: 1px solid #eee;
  border-right: 1px solid #bbb;
  border-bottom: 1px solid #bbb;
}
.rightBottom .chooseWrap dl dd:first-of-type {
  color: red;
}

```

#### JavaScript：

```js
//数据 
  var goodData = {
    goodsDetail: {
      crumbData:
        [
          {
            "title": "选择颜色",
            "data": [
              {
                type: "金色"
              },
              {
                type: "银色"
              },
              {
                type: "黑色"
              },
            ]
          },
          {
            "title": "内存容量",
            "data": [
              {
                type: "16G"
              },
              {
                type: "64G"
              },
              {
                type: "128G"
              },
              {
                type: "256G"
              },
            ]
          },
          {
            "title": "选择版本",
            "data": [
              {
                type: "公开版"
              },
              {
                type: "移动版"
              }
            ]
          },
          {
            "title": "购买方式",
            "data": [
              {
                type: "官方标配"
              },
              {
                type: "优惠移动版"
              },
              {
                type: "电信优惠版"
              },
            ]
          }
        ]
    }
  }


  //逻辑
  //商品参数数据的动态渲染
  rightBottomData();
  function rightBottomData() {
    /**
     * 思路：
     * 1、找rightBottom的元素对象
     * 2、查找data.js->goodData.goodsDetail.crumbData数据
     * 3、由于数据是一个数组，需要遍历，有一个元素则需要有一个动态的dl元素对象(dt、dd)
     */

    //1、查找元素对象
    var chooseWrap = document.querySelector('#right .rightBottom .chooseWrap');

    //2、查找数据
    var crumbData = goodData.goodsDetail.crumbData;

    //3、循环数据
    for (var i = 0; i < crumbData.length; i++) {

      //4、创建dl元素对象
      var dlNode = document.createElement('dl');

      //5、创建dt元素对象
      var dtNode = document.createElement('dt');
      dtNode.innerText = crumbData[i].title;

      //6、dl追加dt
      dlNode.appendChild(dtNode);

      //7、遍历crumbData->data元素
      for (var j = 0; j < crumbData[i].data.length; j++) {

        //创建dd元素
        var ddNode = document.createElement('dd');
        ddNode.innerText = crumbData[i].data[j].type;

        //让dl来追加dd
        dlNode.appendChild(ddNode);
      }

      //8、让chooseWrap追加dl
      chooseWrap.appendChild(dlNode);
    }
  }

  //点击商品参数之后的颜色排他效果
  clickddBind();
  function clickddBind() {
    /**
     * 每一行dd文字颜色排他
     * 思路：
     * 1、获取所有的dl元素，取其中第一个dl元素下的所有dd先做测试，
     *   测试完毕之后在对应dl第一行下标的前面在嵌套一个for循环，目的在变换下标
     * 2、循环所有的dd元素并且添加点击事件
     * 3、确定实际发生事件的目标源对象设置其文字颜色为红色，然后给其他所有的元素颜色都重置为基础颜色(#666)
     * ==========================================================================================
     * 
     * 
     * 点击dd之后产生的mark标记
     * 思路：
     * 1、首先先来创建一个可以容纳点击的dd元素值的容器（数组），确定数组的起始长度,在添加一些默认值
     * 2、然后再将点击的dd元素的值按照对应下标来写入到数组的元素身上
     */

    //1、找第一个dl下的所有的dd元素
    var dlNodes = document.querySelectorAll('#right .rightBottom .chooseWrap dl');

    var arr = new Array(dlNodes.length);

    var choose = document.querySelector('#right .rightBottom .choose');

    //数组填充值
    arr.fill(0);

    // console.log(arr); // [0, 0, 0, 0]

    for (var i = 0; i < dlNodes.length; i++) {

      (function (i) {

        var ddNodes = dlNodes[i].querySelectorAll('dd');

        //2、遍历当前所有的dd元素
        for (var j = 0; j < ddNodes.length; j++) {

          ddNodes[j].onclick = function () {
            // console.log(i);
            //  console.log(ddNodes[i]); // undefined
            //this：表示哪一个元素真实的发生了事件
            // console.log(this);

            //清空choose元素
            choose.innerHTML = "";

            for (var k = 0; k < ddNodes.length; k++) {
              ddNodes[k].style.color = "#666";
            }

            //假设点击的是第二个元素，下标为1'
            //ddNodes[1].style.color = "red";
            //相同下标的dd元素的字体颜色在进行覆盖操作,而其他未点击的元素都是在进行重新设置颜色
            this.style.color = "red";


            //点击哪一个dd元素动态的产生一个新的mark标记元素
            arr[i] = this.innerText;

            //遍历arr数组，将非0元素的值写入到mark标记
            arr.forEach(function (value, index) {
              //只要是为真的条件，咱们就动态的来创建mark标签
              if (value) {
                //创建div元素
                var markDiv = document.createElement('div');
                //并且设置class属性
                markDiv.className = 'mark';
                //并且设置值
                markDiv.innerText = value;
                //创建a元素
                var aNode = document.createElement('a');
                //并且设置值
                aNode.innerText = 'X';
                //并且设置下标
                aNode.setAttribute('index', index);
                //让div追加a 
                markDiv.appendChild(aNode);

                //让choose元素追加div
                choose.appendChild(markDiv);

              }
            })

            //获取所有的a标签元素，并且循环发生点击事件
            var aNodes = document.querySelectorAll('#right .rightBottom .choose .mark a');

            for (var n = 0; n < aNodes.length; n++) {
              aNodes[n].onclick = function () {
                //获取点击的a标签身上的index属性值
                var idx1 = this.getAttribute('index');

                //恢复数组中对应下标元素的值
                arr[idx1] = 0;

                //查找对应下标的那个dl行中的所有的dd元素
                var ddlist = dlNodes[idx1].querySelectorAll('dd');

                //遍历所有的dd元素
                for (var m = 0; m < ddlist.length; m++) {
                  //其余所有dd的文字颜色为灰色
                  ddlist[m].style.color = "#666";
                }

                //默认的第一个dd文字颜色恢复成红色
                ddlist[0].style.color = 'red';

                //删除对应下标位置的mark标记
                choose.removeChild(this.parentNode);
              }
            }

          }
        }
      })(i)
    }
  }
```



### 数字计算总数功能（已废弃）

效果：

![image-20221109084407251.png](https://s2.loli.net/2022/11/10/pQB9RaFs8PjnGZu.png)

![image-20221109084423045](C:\Users\28154\AppData\Roaming\Typora\typora-user-images\image-20221109084423045.png)

```
用法：
1.根据右边选择从而动态改变价格
2.其实就是在《点击参数动态形成结果》的基础上添加参数动态合成总数的功能
3.
```

#### HTML:

```html
<!--右侧商品详情区域开始-->
        <div class="price" style="color: red; font-size: 100px;">
            <p>5299</p>
        </div>
          <div id="right">
            <!--商品参数的结构布局开始-->
            <div class="rightBottom">
              <!--选择之后结果的区域开始-->
              <div class="choose"></div>
              <!--选择之后结果的区域结束-->

              <!--商品参数的选择区域开始-->
              <div class="chooseWrap"></div>
              <!--商品参数的选择区域结束-->

              <!--加入购物车以及数量区域开始-->
              <!--加入购物车以及数量区域结束-->
            </div>
            <!--商品参数的结构布局结束-->
          </div>
```

#### CSS:

```cs
.rightBottom {
  margin-top: 10px;
  line-height: 28px;
}
.rightBottom .choose::after {
  clear: both;
  display: block;
  content: '';
}
.rightBottom .choose .mark {
  height: 30px;
  background: snow;
  border: 1px solid #ddd;
  padding: 0 20px;
  margin-right: 20px;
  text-align: center;
  line-height: 30px;
  float: left;
}
.rightBottom .choose .mark a {
  color: red;
  margin-left: 20px;
}
.rightBottom .chooseWrap dl {
  margin-top: 13px;
  margin-bottom: 13px;
}
.rightBottom .chooseWrap dl::after {
  clear: both;
  display: block;
  content: '';
}
.rightBottom .chooseWrap dl dt {
  margin-right: 15px;
  float: left;
}
.rightBottom .chooseWrap dl dd {
  float: left;
  margin-right: 5px;
  color: #666;
  line-height: 24px;
  padding: 2px 14px;
  border-top: 1px solid #eee;
  border-left: 1px solid #eee;
  border-right: 1px solid #bbb;
  border-bottom: 1px solid #bbb;
}
.rightBottom .chooseWrap dl dd:first-of-type {
  color: red;
}

```

#### JavaScript：

```js
//数据
var goodData = {
  goodsDetail: {
    price: 5299,
    crumbData:
      [
        {
          "title": "选择颜色",
          "data": [
            {
              type: "金色",
              changePrice: 0
            },
            {
              type: "银色",
              changePrice: 40
            },
            {
              type: "黑色",
              changePrice: 90
            },
          ]
        },
        {
          "title": "内存容量",
          "data": [
            {
              type: "16G",
              changePrice: 0
            },
            {
              type: "64G",
              changePrice: 300
            },
            {
              type: "128G",
              changePrice: 900
            },
            {
              type: "256G",
              changePrice: 1300
            },
          ]
        },
        {
          "title": "选择版本",
          "data": [
            {
              type: "公开版",
              changePrice: 0
            },
            {
              type: "移动版",
              changePrice: -1000
            }
          ]
        },
        {
          "title": "购买方式",
          "data": [
            {
              type: "官方标配",
              changePrice: 0
            },
            {
              type: "优惠移动版",
              changePrice: -240
            },
            {
              type: "电信优惠版",
              changePrice: -390
            },
          ]
        }
      ]
  }
}


//逻辑
//商品参数数据的动态渲染
  rightBottomData();
  function rightBottomData() {
    /**
     * 思路：
     * 1、找rightBottom的元素对象
     * 2、查找data.js->goodData.goodsDetail.crumbData数据
     * 3、由于数据是一个数组，需要遍历，有一个元素则需要有一个动态的dl元素对象(dt、dd)
     */

    //1、查找元素对象
    var chooseWrap = document.querySelector('#right .rightBottom .chooseWrap');

    //2、查找数据
    var crumbData = goodData.goodsDetail.crumbData;

    //3、循环数据
    for (var i = 0; i < crumbData.length; i++) {

      //4、创建dl元素对象
      var dlNode = document.createElement('dl');

      //5、创建dt元素对象
      var dtNode = document.createElement('dt');
      dtNode.innerText = crumbData[i].title;

      //6、dl追加dt
      dlNode.appendChild(dtNode);

      //7、遍历crumbData->data元素
      for (var j = 0; j < crumbData[i].data.length; j++) {

        //创建dd元素
        var ddNode = document.createElement('dd');
        ddNode.innerText = crumbData[i].data[j].type;
        ddNode.setAttribute('price', crumbData[i].data[j].changePrice);
        //让dl来追加dd
        dlNode.appendChild(ddNode);
      }

      //8、让chooseWrap追加dl
      chooseWrap.appendChild(dlNode);
    }
  }

  //点击商品参数之后的颜色排他效果
  clickddBind();
  function clickddBind() {
    /**
     * 每一行dd文字颜色排他
     * 思路：
     * 1、获取所有的dl元素，取其中第一个dl元素下的所有dd先做测试，
     *   测试完毕之后在对应dl第一行下标的前面在嵌套一个for循环，目的在变换下标
     * 2、循环所有的dd元素并且添加点击事件
     * 3、确定实际发生事件的目标源对象设置其文字颜色为红色，然后给其他所有的元素颜色都重置为基础颜色(#666)
     * ==========================================================================================
     * 
     * 
     * 点击dd之后产生的mark标记
     * 思路：
     * 1、首先先来创建一个可以容纳点击的dd元素值的容器（数组），确定数组的起始长度,在添加一些默认值
     * 2、然后再将点击的dd元素的值按照对应下标来写入到数组的元素身上
     */

    //1、找第一个dl下的所有的dd元素
    var dlNodes = document.querySelectorAll('#right .rightBottom .chooseWrap dl');

    var arr = new Array(dlNodes.length);

    var choose = document.querySelector('#right .rightBottom .choose');

    //数组填充值
    arr.fill(0);

    // console.log(arr); // [0, 0, 0, 0]

    for (var i = 0; i < dlNodes.length; i++) {

      (function (i) {

        var ddNodes = dlNodes[i].querySelectorAll('dd');

        //2、遍历当前所有的dd元素
        for (var j = 0; j < ddNodes.length; j++) {

          ddNodes[j].onclick = function () {
            // console.log(i);
            //  console.log(ddNodes[i]); // undefined
            //this：表示哪一个元素真实的发生了事件
            // console.log(this);

            //清空choose元素
            choose.innerHTML = "";

            for (var k = 0; k < ddNodes.length; k++) {
              ddNodes[k].style.color = "#666";
            }

            //假设点击的是第二个元素，下标为1'
            //ddNodes[1].style.color = "red";
            //相同下标的dd元素的字体颜色在进行覆盖操作,而其他未点击的元素都是在进行重新设置颜色
            this.style.color = "red";


            //点击哪一个dd元素动态的产生一个新的mark标记元素
            arr[i] = this;

            changePriceBind(arr); //实参

            //遍历arr数组，将非0元素的值写入到mark标记
            arr.forEach(function (value, index) {
              //只要是为真的条件，咱们就动态的来创建mark标签
              if (value) {
                //创建div元素
                var markDiv = document.createElement('div');
                //并且设置class属性
                markDiv.className = 'mark';
                //并且设置值
                markDiv.innerText = value.innerText;
                //创建a元素
                var aNode = document.createElement('a');
                //并且设置值
                aNode.innerText = 'X';
                //并且设置下标
                aNode.setAttribute('index', index);
                //让div追加a 
                markDiv.appendChild(aNode);

                //让choose元素追加div
                choose.appendChild(markDiv);

              }
            })

            //获取所有的a标签元素，并且循环发生点击事件
            var aNodes = document.querySelectorAll('#right .rightBottom .choose .mark a');

            for (var n = 0; n < aNodes.length; n++) {
              aNodes[n].onclick = function () {
                //获取点击的a标签身上的index属性值
                var idx1 = this.getAttribute('index');

                //恢复数组中对应下标元素的值
                arr[idx1] = 0;

                //查找对应下标的那个dl行中的所有的dd元素
                var ddlist = dlNodes[idx1].querySelectorAll('dd');

                //遍历所有的dd元素
                for (var m = 0; m < ddlist.length; m++) {
                  //其余所有dd的文字颜色为灰色
                  ddlist[m].style.color = "#666";
                }

                //默认的第一个dd文字颜色恢复成红色
                ddlist[0].style.color = 'red';

                //删除对应下标位置的mark标记
                choose.removeChild(this.parentNode);

                //调用价格函数
                changePriceBind(arr);
              }
            }

          }
        }
      })(i)


    }

  }

  //价格变动的函数声明
  /**
   * 这个函数是需要在点击dd的时候以及删除mark标记的时候才需要调用
   */
  function changePriceBind(arr) {
    /**
     * 思路：
     * 1、获取价格的标签元素
     * 2、给每一个dd标签身上默认都设置一个自定义的属性，用来记录变化的价格
     * 3、遍历arr数组，将dd元素身上的新变化的价格和已有的价格（5299）相加
     * 4、最后将计算之后的结果重新渲染到p标签中
     */

    //1、原价格标签元素
    var oldPrice = document.querySelector('.price p');

    //取出默认的价格
    var price = goodData.goodsDetail.price;

    //2、遍历arr数组
    for (var i = 0; i < arr.length; i++) {
      if (arr[i]) {
        //数据类型的强制转换
        var changeprice = Number(arr[i].getAttribute('price'));
        //最终价格
        price += changeprice;
      }

    }
    oldPrice.innerText = price;
  }
```



### 购物车样式

效果：

![image-20221109092155388.png](https://s2.loli.net/2022/11/10/xBLr4EHnTavqFPC.png)

#### HTML:

```HTML
<div class="addCart">
    <div class="count">
      <input type="text">
      <a href="javascript:;">+</a>
      <a href="javascript:;">-</a>
    </div>
    <button>加入购物车</button>
  </div>
```

#### CSS:

```CSS
.addCart::after {
  clear: both;
  display: block;
  content: '';
}
.addCart .count {
  width: 55px;
  margin-right: 15px;
  position: relative;
  float: left;
}
.addCart .count input {
  width: 38px;
  height: 37px;
  border: 1px solid #ddd;
  color: #555;
}
.addCart .count a {
  width: 15px;
  height: 18px;
  color: #666;
  background: #f1f1f1;
  position: absolute;
  right: 1px;
  text-align: center;
  line-height: 18px;
  border: 1px solid #ddd;
}
.addCart .count a:first-child {
  top: 0px;
}
.addCart .count a:last-child {
  top: 20px;
  border-top: none;
}
.addCart button {
  padding-left: 25px;
  padding-right: 25px;
  height: 36px;
  font-size: 16px;
  background: #e1251b;
  color: #fff;
  border: none;
  float: left;
}
```



### 商品选项卡样式

效果：

![image-20221109092356750.png](https://s2.loli.net/2022/11/10/xwz7sOPklRyUiaV.png) 

#### HTML:

```HTML
<div class="goodsDetailWrap">
                    <!--左边的侧边栏-->
                    <aside class="leftAside">
                         <!--上边-->
                         <div class="asideTop">
                              <h4 class="active">相关分类</h4>
                              <h4>推荐品牌</h4>
                         </div>
                         <!--下边-->
                         <div class="aslideContent">
                             <div class="active">
                                 <ul class="goodslist1">
                                    <li>手机</li>
                                    <li>手机壳</li>
                                    <li>内存卡</li>
                                    <li>iphone配件</li>
                                    <li>贴膜</li>
                                    <li>手机耳机</li>
                                    <li>移动电源</li>
                                    <li>平板电脑</li>
                                 </ul>
                                 <ul class="goodslist2">
                                    <li>
                                        <img src="images/part01.png" alt="">
                                        <span>Apple苹果iPhone 6s (A1699)</span>
                                        <p>¥6088.00</p>
                                        <div class="button">
                                            <a href="javascript:;">加入购物车</a>
                                        </div>
                                    </li>
                                    <li>
                                        <img src="images/part01.png" alt="">
                                        <span>Apple苹果iPhone 6s (A1699)</span>
                                        <p>¥6088.00</p>
                                        <div class="button">
                                            <a href="javascript:;">加入购物车</a>
                                        </div>
                                    </li>
                                    <li>
                                        <img src="images/part01.png" alt="">
                                        <span>Apple苹果iPhone 6s (A1699)</span>
                                        <p>¥6088.00</p>
                                        <div class="button">
                                            <a href="javascript:;">加入购物车</a>
                                        </div>
                                    </li>
                                    <li>
                                        <img src="images/part01.png" alt="">
                                        <span>Apple苹果iPhone 6s (A1699)</span>
                                        <p>¥6088.00</p>
                                        <div class="button">
                                            <a href="javascript:;">加入购物车</a>
                                        </div>
                                    </li>
                                 </ul>
                             </div>
                             <div></div>
                         </div>
                    </aside>
                    <!--右边的结构-->
                    <div class="rightDetail"></div>
                </div>
```

#### CSS:

```CSS
.goodsDetailWrap {
  width: 1200px;
  margin: 30px auto 0;
}
.goodsDetailWrap .leftAside {
  width: 210px;
  border: 1px solid #ccc;
}
.goodsDetailWrap .leftAside::after {
  clear: both;
  display: block;
  content: '';
}
.goodsDetailWrap .leftAside .asideTop::after {
  clear: both;
  display: block;
  content: '';
}
.goodsDetailWrap .leftAside .asideTop h4 {
  width: 105px;
  height: 40px;
  background: #fff;
  float: left;
  text-align: center;
  line-height: 40px;
  border-bottom: 1px solid #ccc;
  border-top: 3px solid #fff;
}
.goodsDetailWrap .leftAside .asideTop h4.active {
  border-top: 3px solid #e1251b;
  width: 105px;
  line-height: 37px;
  text-align: center;
  line-height: 40px;
  border-bottom: none;
}
.goodsDetailWrap .leftAside .aslideContent {
  padding: 10px;
}
.goodsDetailWrap .leftAside .aslideContent div.active ul.goodslist1 li {
  width: 50%;
  border-bottom: 1px dashed #ededed;
  line-height: 28px;
  float: left;
}
.goodsDetailWrap .leftAside .aslideContent div.active ul.goodslist2 {
  margin: 5px 0 15px;
}
.goodsDetailWrap .leftAside .aslideContent div.active ul.goodslist2 li {
  border-bottom: 1px solid #ededed;
}
.goodsDetailWrap .leftAside .aslideContent div.active ul.goodslist2 li img {
  width: 152px;
}
.goodsDetailWrap .leftAside .aslideContent div.active ul.goodslist2 li p {
  font-size: 16px;
  color: #c81623;
}
.goodsDetailWrap .leftAside .aslideContent div.active ul.goodslist2 li .button {
  margin-top: 5px;
  margin-bottom: 5px;
  text-align: center;
}
.goodsDetailWrap .leftAside .aslideContent div.active ul.goodslist2 li .button a {
  color: #8c8c8c;
  padding: 2px 14px;
  line-height: 18px;
  border: 1px solid #8c8c8c;
}
```



### 商品选择样式

效果：

![image-20221109101857308.png](https://s2.loli.net/2022/11/10/43dMGKpbQ7xiFJH.png)

#### HTTML:

```HTML
<div class="rightDetail">
            <!--选择搭配开始-->
            <div class="chooseBox">
              <!--标题-->
              <h4>选择搭配</h4>
              <!--内容-->
              <div class="listWrap">
                <!--左边-->
                <div class="left">
                  <img src="images/l-m01.png" alt="">
                  <p>¥5299</p>
                  <i>+</i>
                </div>
                <!--中间-->
                <ul class="middle">
                  <li>
                    <img src="images/dp01.png" alt="">
                    <span>Feless费勒斯VR</span>
                    <div>
                      <input type="checkbox">
                      <span>50</span>
                    </div>
                  </li>
                  <li>
                    <img src="images/dp02.png" alt="">
                    <span>Feless费勒斯VR</span>
                    <div>
                      <input type="checkbox">
                      <span>50</span>
                    </div>
                  </li>
                  <li>
                    <img src="images/dp03.png" alt="">
                    <span>Feless费勒斯VR</span>
                    <div>
                      <input type="checkbox">
                      <span>50</span>
                    </div>
                  </li>
                  <li>
                    <img src="images/dp04.png" alt="">
                    <span>Feless费勒斯VR</span>
                    <div>
                      <input type="checkbox">
                      <span>50</span>
                    </div>
                  </li>
                </ul>
                <!--右侧-->
                <div class="right">
                  <div>已购0件商品</div>
                  <p>套餐价</p>
                  <i>¥5299</i>
                  <div>
                    <button>加入购物车</button>
                  </div>
                </div>
              </div>
            </div>
            <!--选择搭配结束-->
          </div>
```

#### CSS:

```CSS
.rightDetail {
  width: 980px;
  float: left;
  margin-left: 8px;
}
.rightDetail .chooseBox {
  border: 1px solid #ddd;
  margin-bottom: 15px;
}
.rightDetail .chooseBox h4 {
  border-bottom: 1px solid #ddd;
  background: #f1f1f1;
  color: #333;
  padding: 5px 0 5px 15px;
}
.rightDetail .chooseBox .listWrap {
  height: 170px;
  padding-top: 10px;
}
.rightDetail .chooseBox .listWrap .left {
  width: 127px;
  height: 165px;
  position: relative;
  float: left;
  text-align: center;
}
.rightDetail .chooseBox .listWrap .left img {
  width: 87px;
  margin: 0 auto;
}
.rightDetail .chooseBox .listWrap .left p {
  color: #c81623;
  font-size: 16px;
}
.rightDetail .chooseBox .listWrap .left i {
  position: absolute;
  top: 48px;
  right: -25px;
  font-size: 16px;
}
.rightDetail .chooseBox .listWrap .middle {
  width: 668px;
  height: 165px;
  float: left;
  text-align: center;
}
.rightDetail .chooseBox .listWrap .middle li {
  width: 127px;
  margin-left: 20px;
  margin-right: 20px;
  float: left;
}
.rightDetail .chooseBox .listWrap .middle li img {
  width: 120px;
  height: 130px;
}
.rightDetail .chooseBox .listWrap .right {
  border-left: 1px solid #ddd;
  float: left;
  width: 153px;
  height: 165px;
  padding-left: 20px;
}
.rightDetail .chooseBox .listWrap .right div {
  margin-top: 10px;
  margin-bottom: 10px;
}
.rightDetail .chooseBox .listWrap .right p {
  font-weight: bold;
  margin-bottom: 10px;
}
.rightDetail .chooseBox .listWrap .right i {
  color: #b1191a;
  font-size: 16px;
  margin-bottom: 10px;
}
.rightDetail .chooseBox .listWrap .right button {
  padding: 10px 25px;
  font-size: 16px;
  color: #fff;
  background: #e1251b;
  border: none;
}

```



### 音乐页面

效果：

![image-20221109125750220.png](https://s2.loli.net/2022/11/10/OfdC2qS3kGbrK1A.png)

#### 用法：

```
1.在该页面文件的同级创建musicPage_styleA包，包结构如图所示：（一定要按照图中一样来命名！）
2.本项目需要用到jquery.js，需要自己引入
3.修改播放器上歌曲的名字，就去banner>ul>li>title 修改
```

![image-20221109174143106.png](https://s2.loli.net/2022/11/10/lgkoS76BxJbzLtq.png) 

```
弃稿：（看不懂上面的，就看弃稿）
1.修改首次进入的背景图片，寻找注释“首次进入的背景图片地址”，修改url的值，这里没有命名要求
不建议与其他组件融合使用，建议把他作为单独一个页面
2.修改歌曲，不要修改源码，修改歌曲就去ul>li的datasrc里修改路径，需要注意的是你的每首歌都需要重命名为这种格式，如：第一首歌就改名为1.mp3 第二首歌就改名为：2.mp3；同时需要去修改“这里是点击播放按钮后播放的音乐”注释下的audio 的src的值。
3.修改歌曲背景，跟修改歌曲类似，不要修改源码，在同级建立一个image/music_background/ 包结构，在改包内放图片，然后第一首歌的背景就重命名为1.jpg 同理第二首歌曲的背景图片就重命名为2.jpg
4.修改歌曲logo，在banner里的ul>li>img里修改src的值，这里没有命名要求

注意：
1.本项目需要用到jquery.js，需要自己引入
2.“播放器的样式”不建议修改，建议随着本项目伴生过去。
```



#### HTML:

```HTML
  <div class="banner">
    <!-- /* no-repeat 使得该图片不重复，不平铺之类的 */ -->
    <ul>
      <li datasrc="./musicPage_styleA/audio/1.mp3" class="m1" title="11-22">
        <img src="./musicPage_styleA/image/logo.ico" alt="xx-xx" width="65">
      </li>
      <li datasrc="./musicPage_styleA/audio/2.mp3" class="m2" title="22-33"><img src="./musicPage_styleA/image/logo.ico"
          alt="11-22" width="90"></li>
      <li datasrc="./musicPage_styleA/audio/3.mp3" class="m3" title="33-44"><img src="./musicPage_styleA/image/logo.ico"
          alt="33-44" width="119"></li>
      <li datasrc="./musicPage_styleA/audio/4.mp3" class="m4" title="44-55"><img src="./musicPage_styleA/image/logo.ico"
          alt="44-55" width="90"></li>
      <li datasrc="./musicPage_styleA/audio/5.mp3" class="m5" title="55-66"><img src="./musicPage_styleA/image/logo.ico"
          alt="55-66" width="65"></li>
    </ul>
  </div>

  <div class="music">
    <div class="m_img">
      <img src="./musicPage_styleA/image/logo.ico" width="90" alt="">
    </div>
    <div class="m_text">Lorem ipsum</div>
    <div class="m_btn">
      <a href="#" class="m_prev" title="上一首"></a>
      <a href="#" class="m_play" title="播放"></a>
      <a href="#" class="m_next" title="下一首"></a>
    </div>
    <div class="m_close "></div>
    <!-- 这里是点击播放按钮后播放的音乐，建议为你的第一首（或者你有能力就改成随机播放） -->
    <audio src="./musicPage_styleA/audio/1.mp3" class="m_mp3"></audio>
  </div>
```

#### CSS:

```CSS
  .banner {
    width: 960px;
    height: 260px;
    margin: 80px;
    opacity: 0.8;
    /* 阴影值 */
    box-shadow: 10px 15px 15px 1px black;
    position: relative;
    /* 子绝父相 */
  }

  body {
    /* 首次进入的背景图片地址 */
    background: url("./musicPage_styleA/image/first_background.jpg") no-repeat;
    background-size: cover;
    /* 自适应，使得该图片填充整个body背景 */
  }

  .banner ul li {
    list-style: none;
    display: inline-block;
    position: absolute;
  }

  .banner ul li img {
    border-radius: 50%;
    /* 图片变圆 */

    cursor: pointer;
    /* 鼠标移到上面变手势 */
  }

  .m1 {
    top: 30px;
    left: 90px;
  }

  .m2 {
    top: 140px;
    left: 160px;
  }

  .m3 {
    top: 67px;
    left: 308px;
  }

  .m4 {
    top: 50px;
    left: 500px;
  }

  .m5 {
    top: 160px;
    left: 580px;
  }

  @keyframes img_rotate {
    from {
      transform: rotate(0deg);
    }

    to {
      transform: rotate(360deg);
    }
  }

  .banner .img_rotate {
    animation: img_rotate 2s infinite linear;
  }

  .music {
    width: 515px;
    height: 105px;
    background: black;
    position: fixed;
    left: 0;
    bottom: 50px;
    opacity: 0.8;
    box-shadow: 10px 15px 15px 1px black;
  }

  .music .m_img {
    margin-top: 15px;
    margin-left: 10px;
    float: left;
    /* 因为div是块级元素，所以加一个左浮动会使得他们从左开始横着排 */
  }

  .music .m_text {
    float: left;
    color: white;
    font-size: 20px;
    font-weight: bold;
    margin-top: 50px;
    margin-left: 20px;

  }

  .music .m_btn {
    float: left;
    /* 防止歌名过长把歌曲名挤掉 */
    position: absolute;
    left: 320px;
  }

  .music .m_btn a {
    float: left;
    width: 35px;
    height: 35px;
    /* 播放器的样式 */
    background: url("./musicPage_styleA/image/player_style.jpg");
    margin-top: 50px;
    margin-left: 20px;
  }

  .music .m_btn .m_prev {
    background-position: -70px 0px;
  }

  .music .m_btn .m_prev:hover {
    background-position: -70px -32px;
  }

  .music .m_btn .m_play {
    background-position: -104px -5px;
  }

  .music .m_btn .m_play:hover {
    background-position: -104px -46px;
  }

  .music .m_btn .m_next {
    background-position: -145px 0px;
  }

  .music .m_btn .m_next:hover {
    background-position: -145px -32px;
  }

  .music .m_close {
    float: right;
    background: url("./musicPage_styleA/image/player_style.jpg");
    width: 21px;
    height: 108px;
    margin-top: 2px;
    cursor: pointer;
  }

  .music .m_btn .m_pause {
    background-position: -292px -93px;
  }

  .music .m_btn .m_pause:hover {
    background-position: -334px -93px;
  }

  .music .m_open {
    background-position: -45px 0px;
  }
```

#### JavaScript：

```js
var index = 0;

  // 获取所有li元素
  var li = $(".banner ul li");

  //获得播放器图片
  var img = $(".music .m_img img");

  //获得
  var text = $(".music .m_text");

  // 
  var prev = $(".music .m_btn .m_prev");
  // 
  var play = $(".music .m_btn .m_play");
  var next = $(".music .m_btn .m_next");

  var mp3 = $(".music .m_mp3");
  // 获取歌曲

  //false 表示播放音乐
  var flag = false;
  var close = true;

  // 获取点击的li索引
  li.click(function () {
    // console.log($(this).index())
    index = $(this).index();
    // 获取当前点击的li索引
    show();

  });

  function show() {
    // 改变背景
    change_bg(index + 1);
    //改变播放器文本
    change_img_text(index + 1);
    //改变播放器按钮
    change_btn_title();
    //图片旋转
    img_rotate();
    //音乐播放
    play_mp3();
  }

  // 更换背景
  function change_bg(idx) {
    $("body").css({
      "background": "url(musicPage_styleA/image/" + idx + ".jpg) no-repeat",
      "background-size": "cover",
    })
  }

  // 这里是更换播放器显示图片和标题
  function change_img_text(idx) {
    img.attr("src", "musicPage_styleA/image/" + idx + ".jpg");
    text.html(li.eq(index).attr("title"))
  }

  // 改变播放器播放的样式
  function change_btn_title() {
    play.removeClass("m_play"); //移除原有的播放样式
    play.addClass("m_pause");
    play.attr("title", "暂停");
  }

  // 让图片开始旋转
  function img_rotate() {
    // 防止全部都旋转
    li.children().removeClass("img_rotate");
    li.eq(index).children().addClass("img_rotate");
  }
  // 让音乐播放
  function play_mp3() {
    mp3.attr("src", li.eq(index).attr("datasrc"));
    mp3.get(0).play();
    // mp3.play();
    flag = true; //歌曲正在播放
  }

  play.click(function () {
    if (flag) {
      mp3.get(0).pause();
      li.eq(index).children().removeClass("img_rotate");
      play.removeClass("m_pause").addClass("m_play").attr("title", "播放");
      flag = false;
    } else {
      mp3.get(0).play();
      li.eq(index).children().addClass("img_rotate");
      play.removeClass("m_play").addClass("m_pause").attr("title", "暂停");
      flag = true;
      change_bg(index + 1); //如果是第一次进入，而且直接点播放按钮的话背景不会更换，所以要这一句处理这个bug
    }
  })

  // 上一首
  prev.click(function () {
    index--;
    if (index < 0) {
      index = li.length - 1;
    }
    show();
  })

  next.click(function () {
    index++;
    if (index > li.length - 1) {
      index = 0;
    }
    show();
  })

  $(".m_close").click(function () {
    if (close) {
      $(this).addClass("m_open");
      $(".music").animate({ "left": "-490px" }, 800)
      close = false;
    }
    else {
      $(this).removeClass("m_open");
      $(".music").animate({ "left": "0px" }, 800)
      close = true;
    }
  })
```

#### 伴生资料（已上传图床）：

player_style.jpg

![player_style.jpg](https://s2.loli.net/2022/11/09/O1Nsz6T7poHjdB5.jpg)







